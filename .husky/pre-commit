#!/bin/sh
# Check staged .mdx files for unsafe braces/brackets
STAGED_MDX=$(git diff --cached --name-only --diff-filter=ACM | grep '\.mdx$' || true)
if [ -n "$STAGED_MDX" ]; then
  node scripts/check-mdx-safety.js $STAGED_MDX
fi
